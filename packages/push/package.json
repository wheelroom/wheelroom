{
  "name": "@wheelroom/push",
  "version": "1.0.0",
  "description": "Push Typescript models to a content platform",
  "exports": {
    "./push": {
      "import": "./build/push.mjs",
      "require": "./build/push.cjs"
    }
  },
  "files": [
    "CHANGELOG.md",
    "build"
  ],
  "scripts": {
    "build": "npm-run-all clean build:*",
    "build:compile": "rollup --config",
    "build:lint": "eslint 'src/**/*.{js,ts,tsx}'",
    "build:test": "jest --silent --passWithNoTests",
    "build:types": "tsc -p tsconfig.packages.json --declaration --emitDeclarationOnly --declarationDir build",
    "clean": "rimraf build/*",
    "dev": "rollup --config --watch"
  },
  "typesVersions": {
    "*": {
      "*": [
        "build/*"
      ]
    }
  }
}