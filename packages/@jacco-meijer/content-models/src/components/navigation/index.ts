/**
 * Component index
 *
 * Component type: navigation
 * Wheelroom type: part
 *
 */

import { fragment } from './graphql'
import { model } from './model'

export const navigation = {
  fragment,
  model,
}
