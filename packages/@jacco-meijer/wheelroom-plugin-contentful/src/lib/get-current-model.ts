import { Component } from '@jacco-meijer/wheelroom'
import { CurrentModel } from '../types/current-model.js'

export const getCurrentModel = (component: Component) => {
  const currentModel = {} as CurrentModel
  return currentModel
}
