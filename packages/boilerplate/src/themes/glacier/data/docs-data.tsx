import { DeepPartial } from '@wheelroom/core'
import { DocsSectionData } from '@wheelroom/wheel-doc'

export const docsSectionData: DeepPartial<DocsSectionData> = {
  docs: {
    hideHeader: false,
    hideHeading: false,
    hideRichText: false,
  },
}
