/**
 * Component index
 *
 * Component type: myNewGlobal
 * Wheelroom type: global
 *
 */

import { query } from './graphql'
import { fragment } from './graphql'
import { model } from './model'

export const myNewGlobal = {
  fragment,
  model,
  query,
}
